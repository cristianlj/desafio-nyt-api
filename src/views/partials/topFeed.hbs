{{#each listOfArticles}}
    <div class="article">
        <div class="text">
            <h2>{{this.title}}</h2>
            <p>{{this.abstract}}</p>
        </div>
        <img src="{{this.media.0.media-metadata.0.url}}">
    </div>
    <br>
{{/each}}
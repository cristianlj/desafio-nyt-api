{{#each listOfArticles}}
    <div class="article">
        <div class="text">
            <a href="{{this.url}}"><h2>{{this.title}}</h2></a>
            <p>{{this.abstract}}</p>
        </div>
        <img src="{{this.multimedia.1.url}}">
    </div>
    <br>
{{/each}}